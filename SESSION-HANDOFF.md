# SESSION HANDOFF — overwritten 2026-07-12 ~19:15 (Claude Code)

## ⚡ CHRONICLE TEST CAMPAIGN (Ken /goal 2026-07-13 ~02:00 — THE standing goal)
Three cumulative gates in ONE fresh-save run: (1) Coherent Colony — 3 hands-off in-game days executing a VillageForge phase-1 plan, beds by night 2, food ≥1-day buffer, a cellar level dug, clean freeze log + screenshots; (2) Living World — diplomacy round with REAL factions + one event propagated into settler dialogue (DB rows + transcript); (3) The Scenario — one doc-11 slice end-to-end, Ken believes the transcript. PARTIAL until all three.
**Campaign state:** Units A (real-terrain export → forge --from-game) and B (faction roster feed → /api/diplomacy/seed) BUILT, staged, 30/30 offline green. **UNIT C NEXT (the headline): PlanExecutor** — mod module consuming forge plan.json phase items → existing actuators (batched wall/door/floor blueprints per rect, cellar digs via DigMarker, fields via FarmPlanner, stockpile zones), idempotent (BuildingExistsAt skip), 50ms-budgeted, progress persisted in BuiltState, REPLACES HouseBuilder improvised siting. Then: deploy window (machine-check first!) → dashboard restart → Gate 1 attempt on a FRESH save with a --from-game phase-1 plan.
VillageForge: v2 phased+3D+styles (LLM = the player, Style block = its voice); v2.2 queue: composite shapes, interiors via the corridor-spine packer, wall-integrated rows.

## ⚡ PREVIOUS (2026-07-13 ~00:10)
**MACHINE IS KEN'S:** he is actively working (Chrome/meshy.ai on primary). ALL game input/window/screenshot automation FROZEN by operator rule until he speaks or a passive sentinel sees world-live. Passive file-only sentinel armed (2h window).
**Late-night defect storm, all root-caused & fixed [deployed]:** (1) native crash ×3 = ScheduleRouter substring mapper wrote RoleJob into Work hours → role-less settlers spun CURRENT_HOUR None → crash; fixed mapper + per-second sanitizer → **None-count now ZERO** [validated]. (2) crashes+1000x query slowdown correlated with MY background-scan threads → **LAW: no game-state reads off the main thread, EVER — slice, don't thread** (catalog+wiki updated); WorldMap scan now sliced-enumerator main-thread (150ms/tick), WorldSense raster DEFERRED (honest no-op until sliced). (3) budget granularity per-ring→per-origin everywhere (67s ring freeze). (4) RESUME-click silent failures = pydirectinput cannot click NEGATIVE-COORDINATE monitors (game lives at x=-1928); fix = move window to primary first — BUT that interfered with Ken's live desktop (own goal); the durable fix belongs in gm_devops/dashboard (move-then-click, or SendMessage-direct). (5) dashboard screen endpoint can capture a CHROME TAB titled "Going Medieval" instead of the game — eyes-on evidence only trustworthy when the game is frontmost [dashboard bug, open].
**Eyes-on discipline (Ken's directive): every watch cycle saves a post-click screenshot; claims are made over pixels.**
**Still unvalidated (the goal's core): beds/shack quest, smokehouse/sewing/palisade pillars, movement orders (draft-first), sliced-scan completion, diplomacy rounds with seeded factions. All queue on the passive sentinel or the next Ken-approved window.**

## ⚡ PREVIOUS STATE (2026-07-12 ~19:55)
**Tonight's campaign arc (Ken's /goal: close the AI-Influence doc gap):** SIX systems deepened, 30/30 offline tests green (test_diplomacy 10, test_disease 7, test_romance 5, test_combat 5, test_events 3): P6 diplomacy agent rounds · P9 disease spread/onset · P7 romance autonomy · P10 combat direction · P5 event lifecycle · P3 REAL movement orders (game's MoveOrder channel — staged). Knowledge triad complete: player-competence-canon (WHAT) + how-things-work (WHY) + **actuator-catalog (HOW — new, docs/ + wiki)**. GameTruthBridge staged as the heartbeat (diplomacy 20min; romance/disease/events 60min; death feed).
**Mod DLL: deployed build = staged-8 (shack/smokehouse/sewing/palisade/crisis/research/caches/bridge-v1); bin is TWO builds ahead (bridge-v2 with events/evolve + romance/tick, MovementOrders)** — deploy at next window. **Dashboard: all five system upgrades on disk, NEED RESTART (Ken's OK — classifier-blocked, correctly).** Ken's 19:02 relaunch never went world-live (menus only, exited 19:35): staged-8 live validation ALSO pending. Observe-only posture holds — do NOT launch the game or restart the dashboard without Ken.
**Next session, in order:** (1) Ken's OK → dashboard restart → mod deploy → launch → validate staged-8 + bridge heartbeat + a diplomacy round + shack rising; (2) movement-order live test (order a settler to a cell, watch them WALK — scenario 1 slice); (3) dialogue features (lie detection, escalation, trading — dashboard dialogue pipeline, fresh eyes); (4) LLM event generation + proclamation-chooser lanes (mod-side budget lanes); (5) doc-11 scenario walkthroughs as the goal's final gate.

## ⚡ PREVIOUS STATE
- **Mod: staged-8 build DEPLOYED (in_sync)** — crisis pop*12, architecture-first research, cross-village cache resets, starter-shack fallback, smokehouse, clothes loop, palisade DefenseBuilder, GameTruthBridge heartbeat+death feed. **NOT yet live-validated** (Ken exited; auto-relaunch collided with his machine use and was STOOD DOWN — observe-only sentinel armed; validate at the next Ken-launched or Ken-approved session).
- **Dashboard: 4 systems upgraded on DISK, 27/27 offline tests green, activation NEEDS A DASHBOARD RESTART (Ken's OK required)** — P6 diplomacy (agent rounds, bounded menus, alliance cascade, raid feed, loss-priced reparations, proclamation→rumor), P9 disease (spread/quarantine/seasonal onset/outbreak rumors), P7 romance autonomy (bonds/weddings the village hears), P10 combat (interventions, diplomacy feed, battle rumors). Suites: test_diplomacy(10) test_disease(7) test_romance(5) test_combat(5).
- **Live-validated by Ken's own 40-min session:** zero freezes under real play; butchering_table built + meat order queued; hunters armed. 
- **Goal campaign (Ken /goal):** gap matrix + arc in BACKLOG (~20:00 → ~22:20 entries, times approximate). Remaining majors: AI-actions game-side movement execution, dialogue lie-detection/escalation/trading, event evolution+personalization, P4 tissue (visits/initiative), infirmary tiers, scenario walkthroughs (doc 11) live.

## One-line state
**Dolgellau LIVE and STABLE (25-min soak passed; long soak running).** Two root causes killed today, both self-inflicted, both VERIFIED fixed: (1) load wedge = GameEventSystem singleton poisoned by menu-time reflection → IsInstantiated() probe; (2) post-live main-thread freeze [4/4 breadcrumb-confirmed] = HouseBuilder's site search (~360k reflected map queries/tick, re-run every tick on a marsh that can never satisfy it) racing the water-sim thread → 50ms time budget + radius-resume + 5-min failure cooldown (CellarBuilder pre-hardened identically). The "RAM wall" was a misread (hang reproduced at 7.7GB free); Ken's Chrome cleanup helped but wasn't the fix. SaveGuard VERIFIED with file evidence (8-min banks on disk). Breadcrumb tracer (validation/tick_phase.txt) stays in — any future freeze names its culprit.

## Hot files
- `src/EventInteractor.cs` — Singleton() now probes `IsInstantiated()` before the getter (THE fix)
- `src/RecapDismisser.cs` — runInBackground force gated OFF during load screen; stuck-load rescue v3 (InvokeLoadingCompleteEvent → 3s → OnContinueClick) kept as backstop; SingletonAudit tripwire logs missing singletons on any stuck load
- `BACKLOG.md` — full arc banked, VERIFIED close + suggested commit title at the tail

## Live hazards / dead theories
- DEAD: host RAM/reboot theory; Steam-only theory; initDone theory; runInBackground-during-load theory (disproven by reproduction with force off + focus held)
- LIVE: `/api/game/input` takes **fractional x,y (0..1)**; `target=` is IGNORED (defaults to center). RESUME = `{x:0.856, y:0.328}`. Mod logs: `%USERPROFILE%\AppData\LocalLow\Foxy Voxel\Going Medieval\LLM_NPCs\logs\`
- LIVE: ~17 other files still use raw reflection `Instance` getters (MainLoop-gated, historically safe) — sweep to a shared poison-proof helper is an open backlog item
- WATCH: telemetry "house:" line says "no dry, flat, open 12x11 footprint near home" — HouseBuilder siting a NEXT structure in marsh; may need elastic site search
- WATCH: title bar showed "(Not Responding)" transiently during a busy-frame screenshot; telemetry stayed fresh — treat as cosmetic unless telemetry stalls

## ⚠ STAGED BUILD (compiled code=0, NOT deployed — Ken mid-session): SEVEN changes
(7th: **DefenseBuilder v1** — palisade ring R=14 on dry approaches only [marsh = free moat], one gate [first dry ring cell — raiders funnel to doors], zone-rect + time-budget + dormant-on-complete guards, Priority 4.65 post-shelter. Validate eyes-on at deploy: ring hugs dry ground, ONE gate, nothing walled through stockpile/house.)
`bin\Release\net472\LLM_NPCs.dll` is ahead of the deployed DLL by: (1) crisis threshold pop*6→pop*12 (canon: 12 raw food/day/settler), (2) ResearchPlanner order architecture→agri→tailor first, (3) cross-village cache resets (WorldMap/WorldSense/HouseSitePlanner — new games were scored against the OLD village's terrain), (4) **STARTER-SHACK fallback** (v2 siting fails + no shelter ever completed → site the legacy 4x7 v1 shack; graduation upgrades later; Ken has NOT yet blessed this — Option A was recommended, veto window open), (5) smokehouse + smoked_meat queue once butcher exists, (6) sewing_station + seasonal clothes queues once shelter complete. Deploy at next natural restart (`/api/dev/build {deploy:true}`), then VALIDATE each live. The player-competence canon (Ken's guide, distilled) lives at `F:\StarForge\wiki\going-medieval\player-competence-canon.md` — READ IT before any coherence work; it carries the mod-gap list (defense, room types, book protection, seeds, fishing) and hard physics for V2/V3 (stability=4 −1/step, beams 15 wood post-Architecture, marsh cellars FLOOD).

## ⚠ CONTEXT CHANGE: Ken started a NEW GAME — Hexham (Marsh, small, seed 901811147)
Ken is DRIVING — no agent screen/input calls until he's done (operator rule 2). Passive file-only observer may be running (reads telemetry + mod log only). Hexham = clean first-boot validation of the full chain incl. the new butcher link. Marsh gaps known: no fishing behavior validated (recon: Fishing job IS crisis-routed prio 1, FishingGoal needs zero designation — if Hexham's crisis fires and nobody fishes, instrument FishingGoal's selection, don't code blind); butcher/stockpile placement lacks reachability (spec with exact API in BACKLOG: IsRegionReachable); region-scale plot siting still the real marsh-housing cure.

## ⚠ NEW GOAL (Ken /goal ~20:00): close the gap to the AI-Influence reference docs (Desktop\X4 AI Influence\...Going Medieval\00-11); end state = doc 11's five scenarios live + coherent NPCs. GAP MATRIX in BACKLOG (~20:00 entry). Campaign order: P6 diplomacy → events slice 2 → AI-actions vocabulary → disease → romance → combat direction.
P6 STATUS — READ THE ~20:45 BACKLOG ENTRY FIRST: **gm_systems.py already has a P6 substrate** (faction_relations/diplomacy_log + apply_diplomacy_action + run_diplomacy_round + events-pipeline emission). My gm_diplomacy.py (same dir) is a DUPLICATE built before full reconciliation — now a REFERENCE IMPLEMENTATION whose unique pieces must be MERGED INTO gm_systems: legal-move menus (bounded LLM choice — the core safety property), alliance-shatter cascade, loss-ratio reparations, raid ground-truth feed, round-robin mover, banishment, and the 10/10 test suite (test_diplomacy.py — port to drive gm_systems; offline green = merge exit gate). Design doc at docs/P6_DIPLOMACY_DESIGN.md (3 Ken sign-off questions at tail). THEN the dashboard-restart-gated slice: round scheduler + LLM chooser (lane `diplomacy`) + mod-side faction roster feed → live validation (raid → war → proclamation in a settler's dialogue).

## Next unit's first command
If the game is live (mod log in LocalLow fresh <150s): proceed straight to **V1 STAIRS** (vertical-construction spec, BACKLOG "SPECIFIED: VERTICAL CONSTRUCTION (S3)") — resolve stair blueprint ids (`wood_stair_straight` etc. are in the BuildingIds dump in any boot log), place via the existing blueprint path, validate settlers build AND climb (eyes on). If dead: relaunch via dashboard, RESUME with `{action:"click", x:0.856, y:0.328}` at boot+120s focused; loads self-heal.
Also queued: region-scale village-plot siting spec (the REAL fix for the marsh — search always fails there by design now, 5-min cooldown keeps it cheap); ◐ items (multi-dialog re-arm, budget-deferred event retry, elastic wood radius); doc-11 scenario coherence checks; reflection-Instance sweep to the poison-proof helper (~17 files).
DELIVERED already: village proposal (validation/village_proposal_dolgellau.html); architect chain validated end-to-end (design → 12×11 common house BUILT, eyes-on).

## Eyeball queue (Ken, 30 seconds each)
1. **Longhouse complete** — load the game view; the dark-roofed longhouse by the plaza, door facing the stockpiles. Confirm it reads as a real village building, not a shed.
2. Food crisis banner — top-right alerts; reactor is foraging. Confirm settlers aren't starving over the next day.

## Commit question
**Uncommitted work is stacked across the whole arc — commit point NOW.** Suggested title for this close: `fix(load): kill singleton-poisoning load wedge at root; poison-proof reflection access, stuck-load rescue + audit as backstop`. Earlier pre-written titles are in BACKLOG.md at each close.
